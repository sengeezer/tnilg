import React from 'react';

export default () => {
  return (
    <div>
      <h2>How to use</h2>
      <ol>
        <li>To add a feed, enter the URL of a feed into the URL bar and hit Submit.</li>
        <li>
          To select a feed, click on its URL in the list below the URL field. If you only
          have one feed in the list, its posts will appear here by default.
        </li>
      </ol>
    </div>
  );
};
